source "https://rubygems.org"

# Pinned for reproducible installs. The committed Gemfile.lock (generate it on your
# Mac with `bundle install`) pins the full dependency tree exactly.
gem "fastlane", "~> 2.237"
